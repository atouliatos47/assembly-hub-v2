services:
  - type: web
    name: assembly-hub
    runtime: python
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: python server.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.0
